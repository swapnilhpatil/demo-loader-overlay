import Home from './components/Home/index';

/**
 * Root application component.
 * Renders the Home page which contains the full loader-overlay demo experience.
 *
 * @returns {JSX.Element} The root App layout
 */
export default function App() {
  return (
    <Home />
  );
}
